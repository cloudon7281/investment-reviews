# Capped below 4.0.0 deliberately.  The original reason is gone — yfinance no longer
# calls pd.Timestamp.utcnow() — but the argument for the cap never depended on that one
# call: pandas is where every figure in this tool lives, an unbounded major means the
# next image rebuild after a pandas 4 release picks it up, and the break would arrive at
# 22:05 on a night nobody chose rather than when someone decided to upgrade.  Our own
# code raises no deprecation warnings under pandas 3, so lifting this is a small and
# schedulable piece of work, which is exactly the point of the cap.
pandas>=2.0.0,<4.0.0
numpy>=1.24.0
yfinance>=1.7.0,<2.0.0
beautifulsoup4>=4.12.0
tabulate>=0.9.0
openpyxl>=3.1.2
python-dateutil>=2.8.2
lxml>=4.9.3
numbers-parser>=4.15.1
PyYAML>=6.0
pyxirr>=0.10.0
pdfplumber>=0.10.0
reportlab>=4.0.0
google-api-python-client>=2.100.0
google-auth>=2.23.0